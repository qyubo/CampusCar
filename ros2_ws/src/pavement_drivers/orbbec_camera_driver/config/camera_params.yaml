orbbec_camera_driver:
  ros__parameters:
    backend: "mvs"  # auto, mvs, opencv, test; real-camera launch must use mvs
    camera_ip: "10.7.142.16"
    serial_number: ""
    mvs_python_path: ""  # 可填 MvCameraControl_class.py 所在目录
    frame_id: "camera_frame"
    fps: 2.0
    source: ""  # backend=opencv 时填视频文件、RTSP 地址或摄像头索引
    width: 640
    height: 480
    grab_timeout_ms: 1000
