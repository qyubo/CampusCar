# PWM切换组合参数使用说明

## 1. 适用范围

本文档对应当前工程的默认 `VARIANT_USART` 固件配置：

```c
#define CONTROL_SERIAL_USART2 0
#define FEEDBACK_SERIAL_USART2
#define DUAL_INPUTS
#define CONTROL_PWM_RIGHT 1
```

系统开机默认由USART2控制，右侧双通道PWM作为人工辅助输入。PWM不能仅靠输出普通摇杆值直接接管，必须先完成规定的组合手势。

本文所有参数以当前 `Inc/config.h`、`Src/control.c` 和 `Src/util.c` 为准。

## 2. 接口与电气连接

### 2.1 驱动板引脚

| PWM通道 | STM32引脚 | 功能 |
|---|---|---|
| CH1 | PB10 | 转向 |
| CH2 | PB11 | 速度 |

ESP32示例连接：

| ESP32 | 驱动板 | 说明 |
|---|---|---|
| GPIO25 | PB10 | CH1转向PWM |
| GPIO26 | PB11 | CH2速度PWM |
| GND | GND | 必须共地 |

ESP32的3.3 V PWM逻辑电平可以直接用于当前右侧输入。右侧接口还带有驱动板12/15 V电源线，不得直接连接ESP32的3.3 V或5 V引脚。ESP32应通过规格合适的降压模块供电。

不同主板线序和线色可能不同，应根据主板原理图或实测确认PB10、PB11、GND和电源线，不能只按线色连接。

## 3. PWM基础参数

推荐使用标准RC PWM：

```text
频率：50 Hz
周期：20 ms
高电平脉宽：1000~2000 us
```

脉宽映射：

| 高电平脉宽 | 固件捕获值 | 标准化命令 |
|---:|---:|---:|
| 1000 us | 0 | -1000 |
| 1500 us | 500 | 0 |
| 2000 us | 1000 | +1000 |

换算关系：

```text
捕获值 = clamp(脉宽us, 1000, 2000) - 1000
标准化命令 = (捕获值 - 500) * 2
```

固件接受的原始高电平脉宽范围是 `900~2100 us`，但接受后会限幅到 `1000~2000 us`。ESP32端仍应只输出 `1000~2000 us`。

## 4. PWM信号有效判定

当前参数：

| 参数 | 当前值 | 含义 |
|---|---:|---|
| `PWM_SIGNAL_MIN_US` | 900 us | 合法脉宽下限 |
| `PWM_SIGNAL_MAX_US` | 2100 us | 合法脉宽上限 |
| `PWM_SIGNAL_VALID_FRAMES` | 3 | 两通道连续合法脉冲计数门槛 |
| `PWM_SIGNAL_PERIOD_MAX_MS` | 35 ms | 两通道最近合法帧允许的最大时间差 |
| `PWM_SIGNAL_TIMEOUT_MS` | 150 ms | 任一通道丢失后的失效时间 |

CH1、CH2必须都持续输出。任一通道超过约150 ms没有合法脉冲时：

```text
pwm_signal_valid = 0
两通道内部值归中
PWM活动时电机立即进入安全零输出/OPEN_MODE
```

注意：配置中虽然存在 `PWM_SIGNAL_PERIOD_MIN_MS=8`，当前源码没有真正使用它检查单通道周期；`PWM_SIGNAL_PERIOD_MAX_MS=35`当前实际用于检查两通道最近合法帧的时间差。因此外部控制器应固定使用50 Hz，不应输出高频窄间隔脉冲。

## 5. 手势开始前的中位预备

组合手势前必须先让两路PWM保持中位：

| 项目 | 条件 |
|---|---|
| CH1预备窗口 | `1450~1550 us` |
| CH2预备窗口 | `1450~1550 us` |
| 预备帧数 | PWM有效后至少4组双通道帧 |

50 Hz下，从刚开始输出两路PWM到完成信号稳定和中位预备，理论上需要约160 ms。实际控制建议保持中位至少300~500 ms。

如果在 `pwm_signal_ready` 建立前离开中位，预备帧计数会清零。预备完成后才允许识别“上”动作。

## 6. 上下左右组合参数

手势顺序固定为：

```text
中位预备 -> 速度上 -> 速度下 -> 转向左 -> 转向右
```

严格判定和推荐输出值：

| 步骤 | 固件严格条件 | CH1转向 | CH2速度 |
|---|---|---:|---:|
| 中位预备 | 两通道均在`1450~1550 us` | 1500 us | 1500 us |
| 上 | CH2 `> 1800 us` | 1500 us | 1900 us |
| 下 | CH2 `< 1200 us` | 1500 us | 1100 us |
| 左 | CH1 `< 1200 us` | 1100 us | 1500 us |
| 右 | CH1 `> 1800 us` | 1900 us | 1500 us |
| 完成后 | 立即安全回中 | 1500 us | 1500 us |

不能把1800 us或1200 us本身作为动作值，因为源码使用严格的大于和小于判断。推荐1900 us和1100 us，为测量误差保留余量。

### 6.1 时间要求

每个方向被识别后，下一方向必须在约2秒内出现：

```c
#define GESTURE_TIMEOUT 400
主循环周期约5 ms
```

外部控制器推荐：

```text
每个方向保持：80~150 ms
方向之间回中：约100 ms
完整动作时间：约1秒
```

固件不强制方向之间回中，但插入中位可以让脉冲变化更清晰，并减少方向判断受上一动作影响的可能。

### 6.2 完成提示

识别最后“右”动作后：

1. 固件发出4声短鸣，表示组合完成。
2. 活动输入从USART切换为PWM。
3. 再发出8声提示，表示已经进入PWM模式。
4. PWM最高转速限制切换为当前源码中的300 rpm。

源码中的部分注释和打印仍写着500 rpm，但实际赋值是：

```c
rtP_Left.n_max = rtP_Right.n_max = 300 * 16;
```

## 7. 最后“向右”动作的安全要求

固件在识别“右”的同一轮处理中立即切换到PWM。因此最后的 `CH1=1900 us` 不再只是密码的一部分，也可能在后续控制循环中成为真实右转命令。

外部控制器必须：

- 将最后“右”保持时间控制在约40~80 ms。
- 完成后立即输出两路1500 us。
- 首次调试时架空驱动轮。
- 不允许设备周围有人或障碍物。

从固件安全设计角度，更严格的后续改进是增加“组合完成后必须再次稳定回中，才正式开放PWM输出”的状态；当前版本尚未实现这个二次确认。

## 8. PWM模式保持与切回USART

PWM活动期间，以下任一通道超出活动中位区间，就会持续重置10秒退出计时：

```text
CH1 < 1400 us 或 CH1 > 1600 us
CH2 < 1400 us 或 CH2 > 1600 us
```

当两路都保持在 `1400~1600 us` 内时，不再重置退出计时。持续约10秒后自动切回USART。

这里有两个不同的中位范围：

| 用途 | 范围 |
|---|---|
| 手势开始前的严格预备中位 | `1450~1550 us` |
| PWM模式退出计时中位 | `1400~1600 us` |

PWM任一通道丢失超过150 ms时，电机先立即安全归零；如果信号持续丢失，10秒退出计时继续运行，达到时间后切回USART。

切回USART后：

- USART在线且持续有合法帧时，恢复USART控制。
- USART离线或已经超时时，保持 `OPEN_MODE` 和零命令。
- 后续收到合法USART运动帧或控制帧后自动恢复，不需要再次执行PWM组合。
- 切回时发出18声提示。

## 9. 开机PWM松杆模式选择

当前默认模式：

```c
#define PWM_RELEASE_DEFAULT_MODE PWM_RELEASE_SOFT_DECEL
```

固件启动约400 ms后读取速度通道：

| 启动时CH2脉宽 | 选择结果 |
|---:|---|
| `>1850 us` | `PWM_RELEASE_SOFT_DECEL` |
| `<1150 us` | `PWM_RELEASE_NORMAL` |
| 其他值 | 使用默认值，当前为`PWM_RELEASE_SOFT_DECEL` |

ESP32示例在启动后持续输出1500 us，因此不会触发高位或低位覆盖，使用固件默认模式。

## 10. ESP32示例

示例文件：

```text
Arduino/esp32_pwm_gesture/esp32_pwm_gesture.ino
```

默认引脚：

```text
GPIO25 -> CH1/PB10/转向
GPIO26 -> CH2/PB11/速度
GPIO32 -> 可选组合触发按钮，按钮另一端接GND
```

示例兼容Arduino-ESP32 2.x和3.x的LEDC API，输出50 Hz、16位分辨率PWM。上电只输出中位，不会自动执行组合。

串口监视器命令：

| 命令 | 功能 |
|---|---|
| `g` | 执行中位、上、下、左、右组合并回中 |
| `w` | 小幅前进测试 |
| `s` | 小幅后退测试 |
| `a` | 小幅左转测试 |
| `d` | 小幅右转测试 |
| `x`或`0` | 两路回中；保持约10秒后固件退回USART |

只有在架空车轮并确认方向正确后，才能使用方向测试命令。

## 11. 推荐ESP32时序

```text
CH1=1500, CH2=1500，保持400 ms
CH1=1500, CH2=1900，保持120 ms    # 上
CH1=1500, CH2=1500，保持100 ms
CH1=1500, CH2=1100，保持120 ms    # 下
CH1=1500, CH2=1500，保持100 ms
CH1=1100, CH2=1500，保持120 ms    # 左
CH1=1500, CH2=1500，保持100 ms
CH1=1900, CH2=1500，保持60 ms     # 右
CH1=1500, CH2=1500                # 立即回中
```

## 12. 联调检查清单

1. 架空驱动轮并准备物理断电手段。
2. 确认ESP32和驱动板共地。
3. 用示波器或逻辑分析仪确认两路均为50 Hz、1500 us中位。
4. 确认没有把驱动板12/15 V接入ESP32 GPIO或电源脚。
5. 发送组合前保持中位至少300~500 ms。
6. 确认组合顺序是速度上、速度下、转向左、转向右。
7. 确认收到4声加8声提示。
8. 组合完成后立即确认两路回到1500 us。
9. 小幅测试前后和左右方向，不正确时先断电再调整映射。
10. 两路回中10秒，确认18声提示并退回USART。
11. 分别断开CH1、CH2，确认约150 ms后电机立即安全归零。
12. USART离线时退回USART，确认系统保持 `OPEN_MODE`，收到合法帧后才恢复。
