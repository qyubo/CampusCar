approval_policy = "never"
sandbox_mode = "danger-full-access"

# This repo is intentionally configured for low-friction local automation.
# It applies only when Codex is started inside this trusted project.
